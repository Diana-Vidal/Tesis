

zonas <- fread("Bases/def_zonas.csv") %>% 
  select(ENTIDAD = ENTIDAD_RES,
         MUN_RES = MUNICIPIO_RES,
         ZONA,
         POB_MUN_RES = POB_MUNI_RES,
         DEFUNCIONES = Defunciones,
         SE,
         FECHASE = FechaSE)

head(zonas)
